@{
    Layout = "_Layout";
    var result = ((IEnumerable<Tuple<int, string>>)Model)?.ToList();
}

<!DOCTYPE html>

<html>

<head>
    <meta name="viewport" content="width=device-width" />
    <title>Task3</title>
    <style>
        table, th, td {
            border: 1px solid black;
            border-collapse: collapse;
        }

        th, td {
            padding: 5px;
            text-align: left;
        }
    </style>
</head>
<body>
    <h1>Task 3</h1>
    <p>Please enter number from 1 to 100: </p>

    <form method="post" 
          action="~/Tasks/Task3">
        <input type="text"
               name="number"
               value="@ViewBag.UserId" />

        <input type="submit"
               value="Send" />
    </form>
    <br />

    @if (result != null && result.Count > 0)
    {
        <table>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
            @foreach (var tuple in result)
            {
                <tr>
                    <td>@tuple.Item1.</td>
                    <td>@tuple.Item2</td>
                </tr>
            }
        </table>
    }
    else if (ViewBag.UserId > 0)
    {
        <p>No todos for user with ID: @ViewBag.UserId</p>
    }

</body>
</html>
